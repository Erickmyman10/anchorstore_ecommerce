info:
  name: Delete Profile
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/profiles/6"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
