info:
  name: Get All Profiles
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/profiles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
