info:
  name: Get All Users
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
