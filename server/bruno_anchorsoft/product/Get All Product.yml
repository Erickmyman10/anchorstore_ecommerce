info:
  name: "Get All Product "
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
